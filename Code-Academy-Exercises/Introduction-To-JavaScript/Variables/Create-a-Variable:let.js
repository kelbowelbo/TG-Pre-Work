let changeMe=true;
changeMe=false;
console.log(changeMe);

/* When you want to declare a variable, but don't yet have a value, you can declare it
by using the "let" and assigning it a value later. */
