const plantNeedsWater = (day) => {
  if (day === 'Wednesday') {
    return true;
  } else {
    return false;
  }
};

/*I learned that using Arrow Functions, you can write a function without Using
the keyword "function".*/
